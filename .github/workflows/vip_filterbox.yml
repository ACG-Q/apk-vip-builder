name: VIP FilterBox

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/_vip_build.yml
    with:
      app_name: filterbox
      repo_url: ${{ vars.REPO_URL || 'https://github.com/ACG-Q/apk-vip-builder' }}
    secrets: inherit
